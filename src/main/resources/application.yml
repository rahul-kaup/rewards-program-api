# logging
logging:
  level:
    root: info

# database
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rewards_program_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
